---
- name: "Installing additional applications."
  apt: pkg={{item}} state=installed
  with_items:
    - docker
    - aws_cli
    - terraform
    - git
    - wget
    - vim
    - nano
    - zip
    - unzip
    - dnsutils
    - ntp 
